namespace basics_01;

// The default type of an enum is int (32 bits)
// can change the default type as follows to byte (8 bits)
enum WeekEnum : byte
{
    Sunday = 1,
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday
}
